import { FormKitHandlerPayload } from '@formkit/core';

/**
 * Catalog of the error message codes in FormKit.
 * @public
 */
export declare const errors: Record<string | number, string | ((error: FormKitHandlerPayload) => string)>;

/**
 * Register the dev handler (idempotent).
 *
 * @public
 */
export declare function register(): void;

/**
 * Catalog of the warning message codes in FormKit.
 * @public
 */
export declare const warnings: Record<string | number, string | ((error: FormKitHandlerPayload) => string)>;

export { }
